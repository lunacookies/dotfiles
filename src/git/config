# vim: set filetype=gitconfig nomodeline:

[user]
name = Aramis Razzaghipour
email = aramisnoah@gmail.com

# Show the diff of each commit when editing the message.
[commit]
verbose = true

# Prune deleted branches when fetching from a remote.
[fetch]
prune = true
