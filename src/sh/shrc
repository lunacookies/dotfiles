# vim: filetype=bash

alias g=git
alias c=cargo
alias b=brew
alias m='make -j10'

mkcd() {
	mkdir -p "$1"
	cd "$1"
}

makeunirepo() {
	git config user.email u7653557@anu.edu.au
	git config user.signingkey /Users/luna/.ssh/gitlab
}

export PS1="$0$ "
