#!/usr/bin/env bash

playground_dir=/tmp/playground

if [ -e $playground_dir ]; then
    rm -rf $playground_dir
fi

cargo new --vcs=none $playground_dir
cd $playground_dir

code --new-window --folder-uri=file://$playground_dir src/main.rs
code src/main.rs
